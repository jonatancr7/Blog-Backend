import { sequelize } from "../utils/DataBase/configDB.js";
import { DataTypes } from "sequelize";

/*export const comments = sequelize.define("Comments", {
  id: {
    type: DataTypes.UUID,
    defaultValue: DataTypes.UUIDV4,
  },
  description: {
    type: DataTypes.STRING(),
    allowNull: true,
    primaryKey:true,
  },
});*/
